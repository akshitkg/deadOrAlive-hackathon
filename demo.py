# from encryption import generate_key

# generate_key()
# filename="/home/deadOrAlive-hackathon/encryption.py"

# exec(compile(open(filename, "rb").read(), filename, 'exec'), globals, locals)

# generate_key()